# "VibeCoding"

## Add Santa

Add an image of Santa on a sleigh to the frontend UI.

## Commit + push

Pushing should trigger GitHub Actions deployment (watch the workflow run, then check the app).

## Optional: run locally

```shell
@tomasz_mhent ➜ /workspaces/tomasz-base-app (main) $ chmod +x ./app/start.sh
@tomasz_mhent ➜ /workspaces/tomasz-base-app (main) $ ./app/start.sh
```
